# 视频切片工具 使用手册 v2.0

---

## 📌 一、简介

AI 驱动的自动视频切片工具。输入一个长视频，自动完成：
- 📝 AI 语音识别生成字幕
- 🧠 通义千问提取视频大纲
- ✂️ 智能拆分成多个短视频
- 🎬 自动生成合集

---

## 📌 二、服务状态

```bash
cd /Users/zhuobao/.openclaw-rescue4/workspace/video-clipper
bash check-status.sh
```

**当前状态：**
- ✅ 正式版运行中（8000/3000）
- ✅ 测试版运行中（8030/3030）

---

## 📌 三、访问地址

### 3.1 本地访问（Mac 本机上使用）

| 版本 | 前端地址 | 后端地址 |
|------|---------|---------|
| 正式版 | http://localhost:3000 | http://localhost:8000 |
| 测试版 | http://localhost:3030 | http://localhost:8030 |

**操作步骤：**
1. 在 Mac 上打开浏览器（Safari / Chrome）
2. 输入地址：`http://localhost:3000`（正式版）或 `http://localhost:3030`（测试版）
3. 回车访问

---

### 3.2 局域网访问（手机/其他电脑访问 Mac 上的服务）

**本机 LAN IP：** `172.16.120.82`

| 版本 | 局域网访问地址 |
|------|--------------|
| 正式版 | http://172.16.120.82:3000 |
| 测试版 | http://172.16.120.82:3030 |

**操作步骤：**

**手机访问：**
1. 确保手机和 Mac 在同一 WiFi 网络下
2. 打开手机浏览器（微信/Chrome/Safari）
3. 输入：`http://172.16.120.82:3000`
4. 直接使用，无需额外配置

**其他电脑访问：**
1. 确保在同一局域网
2. 打开浏览器
3. 输入：`http://172.16.120.82:3000`

**⚠️ 注意：**
- 如果访问不了，检查 Mac 防火墙设置
- 允许"输入法"或"Screen Sharing"等应用接收网络连接
- 或在"系统设置 → 安全性与隐私 → 防火墙"中添加例外

---

### 3.3 版本对比

| | 正式版 | 测试版 |
|--|--------|--------|
| **版本号** | v1.0 | v1.1-beta |
| **前端端口** | 3000 | 3030 |
| **后端端口** | 8000 | 8030 |
| **数据库文件** | `data/video_clipper.db` | `data/video_clipper_beta.db` |
| **Redis DB** | 0 | 1 |
| **Celery 队列** | `processing` | `processing_beta` |
| **项目数量** | 8 个 | 7 个 |
| **访问地址** | localhost:3000 | localhost:3030 |
| **推荐用途** | 日常剪辑生产 | 新功能测试 |

**数据完全隔离，互不影响。**

---

## 📌 四、启动与停止

### 4.1 启动服务

```bash
cd /Users/zhuobao/.openclaw-rescue4/workspace/video-clipper

# 启动正式版
./start-release.sh

# 或启动测试版
./start-beta.sh
```

### 4.2 停止服务

```bash
# 停止所有相关进程
pkill -f "uvicorn.*8000"
pkill -f "uvicorn.*8030"
pkill -f "celery.*processing"
pkill -f "celery.*processing_beta"
pkill -f "vite.*3000"
pkill -f "vite.*3030"
```

### 4.3 检查服务状态

```bash
cd /Users/zhuobao/.openclaw-rescue4/workspace/video-clipper
bash check-status.sh
```

---

## 📌 五、使用流程

### 5.1 打开前端

根据需求选择：
- 本机使用 → http://localhost:3000
- 手机/其他设备 → http://172.16.120.82:3000

### 5.2 上传视频

1. 点击"选择文件"按钮
2. 选择视频文件（支持 mp4/mov/avi/mkv/flv/webm）
3. 输入项目名称（用于识别区分）
4. 点击上传

**⚠️ 注意：**
- 视频格式建议用 mp4
- 文件扩展名必须与实际格式一致
- 如果上传时提示 400 错误，说明文件格式有问题（扩展名骗人）

### 5.3 开始处理

1. 上传完成后，点击"开始处理"按钮
2. 系统自动按顺序执行：
   - 📝 生成字幕（AI 语音识别）
   - 🧠 提取大纲（通义千问分析）
   - ✂️ 智能切片（拆分成多个短视频）
   - 🎬 生成合集（合并相关片段）

### 5.4 查看结果

- 项目列表中查看所有切片
- 点击单个切片预览
- 可单独下载或批量下载

### 5.5 切片过程说明

| 阶段 | 预计时间 | 说明 |
|------|---------|------|
| 上传视频 | 取决于文件大小 | 可在进度条查看 |
| 生成字幕 | 1-3 分钟 | AI 语音识别 |
| 提取大纲 | 10-30 秒 | 通义千问分析 |
| 切片生成 | 3-10 分钟 | 根据切片数量 |
| 合集生成 | 1-5 分钟 | 自动合并相关片段 |

**提示：** 切片过程可以关闭页面，下次打开继续查看结果。

---

## 📌 六、故障排查

| 问题现象 | 原因 | 解决方法 |
|---------|------|---------|
| 上传失败 400 | 视频扩展名与实际格式不符 | 用真正 mp4 格式的视频 |
| 切片卡住不动 | Worker 未运行 | `bash check-status.sh` 检查，或重启服务 |
| 字幕生成失败 | API Key 配置错误 | 检查 `.env` 中 `DASHSCOPE_API_KEY` |
| 访问 localhost:3000 无响应 | 服务未启动 | `./start-release.sh` 启动 |
| 手机访问不了 | 防火墙拦截 | Mac 上允许网络连接 |
| 两个版本数据混在一起 | 没按脚本启动 | 必须用 `./start-release.sh` 或 `./start-beta.sh` |

---

## 📌 七、相关文件

| 文件 | 用途 |
|------|------|
| `start-release.sh` | 启动正式版 |
| `start-beta.sh` | 启动测试版 |
| `check-status.sh` | 检查服务状态 |
| `DEPLOYMENT.md` | 详细部署文档 |
| `TROUBLESHOOTING.md` | 故障排查指南 |

---

**最后更新：** 2026-06-26
